version: '3' #docker-compose.yml
networks:
  integration:
services:
  jenkins: #JENKINS
    image: jenkins/jenkins:2.138.3
    privileged: true
    user: root
    ports:
      - 8080:8080
      - 50000:50000
    container_name: jenkins
    # entrypoint: ./jenkins/entrypoint.sh
    networks:
    - integration
    volumes:
      - ./jenkins/persistence:/var/jenkins_home:rw
      - ./jenkins/plugins/install_jenkins_plugins.sh:/install_jenkins_plugins.sh
      - ./jenkins/plugins/plugins.txt:/plugins.txt
      - ./jenkins/entrypoint.sh:/entrypoint.sh
    restart: always
  otus_jenkins:
    container_name: otus_jenkins
    image: nginx:1.15.5
    command:
      - 'nginx'
      - '-g'
      - 'daemon off;'
    restart: always
    privileged: true
    user: root
    depends_on:
      - jenkins
    volumes:
      - ./nginx/persistence/sites-enabled:/etc/nginx/sites-enabled/
      - ./nginx/persistence/nginx.conf:/etc/nginx/nginx.conf
    networks:
      - integration
    ports:
      - 80:80
      - 443:443
    links:
      - jenkins
